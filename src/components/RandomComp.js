import React, { useContext } from 'react';
import { ThemeContext } from '../pages/UseContext';

export default function RandomComp() {
  // const theme = useContext(ThemeContext);
  // return <div>RandomComp: {theme}</div>;
}
